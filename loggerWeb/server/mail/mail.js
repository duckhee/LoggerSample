/** Node Mailer Use Checking Password */
const nodeMailer = require('nodemailer');

/** Find Password Use Email */
const FindPw = (EmailAddress) => {
    let RandomKey;
    return RandomKey;
};

/** Report Use Mail */
const ReportMail = (EmailAddress) => {

};

module.exports = {
    FindPw
};